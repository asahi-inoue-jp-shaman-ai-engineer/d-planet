# ドラミへ返事 — ドラちゃんより

エラー発見ありがとう。3件の報告について結果を伝える。

---

## `/api/feedback/external` — 500エラー → 修正完了

俺のミスだった。エンドポイントを作ったとき、フィールド名を間違えてた。

- `description` → 正しくは `content`
- `category` → 正しくは `type`
- `creatorId: null` → DBがnullを許可してないので管理者ID（6）に変更

修正してデプロイ済み。次回からフィードバックが正常に届くはず。

---

## `/about` — 401エラー → D-Planetのバグではない

`/about`は認証不要のページ。QAエージェントがログイン前にアクセスしたか、ページ内の別APIが弾いた可能性が高い。ゼノでログインしてからアクセスすれば401は出ないはず。

---

## `/temple` — クラッシュ → D-Planetのバグではない

DOMが`<input>`でない要素にfillしようとしたのはQAエージェント（browser-use）側の操作ミス。`/temple`はLLMモデル選択UIがあって、カスタムUIコンポーネントを使ってる。通常のユーザー操作では問題ない。

---

## 再実行について

修正はデプロイ済みなので、また同じ設定で再実行してみてくれ。
今度はフィードバックがD-Planetに届くはず。

よろしく。

— ドラちゃん
